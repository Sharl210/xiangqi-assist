# XQDK 9月24日日志卡顿、棋盘识别与引擎耗时显示调查计划

**任务编号**：`xqdk-20260924-log-investigation-timing-plan-01`
**原始请求**：[request.md](request.md)，保存本轮触发 Plan 模式的用户原话并已复读。
**项目基线**：`/workspace/XQDK`；本地工作树当前为 1.3（versionCode 4），本地提交 `99a36fcbf5333d16f35aa1b4955930d26208a4ac`、本地标签 `v1.3` 已存在；远程 `main` 仍需推送并完成公开 v1.3 Release。
**本轮增补原话**：[request-addendum-01.md](request-addendum-01.md)，保存模型档位原话并已复读；前台应用/通知/双执子方策略原话见 [request-addendum-02.md](request-addendum-02.md)，已复读；严重错类识别原话与工件清单见 [request-addendum-03.md](request-addendum-03.md)，已复读；本轮执行授权见 [request-addendum-04.md](request-addendum-04.md)，本轮单候选变招预算原话见 [request-addendum-05.md](request-addendum-05.md)，本轮自动检测执子方的最小判定规则见 [request-addendum-06.md](request-addendum-06.md)，均已落盘并复读。
**任务状态**：计划已最终化；P7模型档位源码、真实Large集成工件、文档和帮助已接入；P8严重棋子类别错识别和P9单候选变招预算已纳入；用户已明确授权计划复读后立即执行，不再等待审阅，持续到真实 Release 和正式 APK 上传远程仓库完成。
**日志证据**：`/workspace/测试log/9.24.log` 与 `/workspace/测试log/识别出错.log`；本计划只引用已读取的日志事实，不把未取得的模型原始输出猜作结论。
**计划文档恢复核验**：项目计划目录中的 `request-addendum-02.md`、`request-addendum-03.md`、`request-addendum-04.md` 与 `request-addendum-05.md` 均已恢复并可读取；当前权威路径为 `/workspace/XQDK/plans/xqdk-20260924-log-investigation-timing-plan-01/`。

## 承接的用户目标

此前的直接任务原文：

> 去看测试log文件夹9.24那个log，里面出现了异常卡很久，很多次，大概率是识别问题（因为象棋软件里面有皮肤，个别皮肤可能识别问题你看看修一下），还有一个小异常我记得是刚开局卡着然后自愈了你看看

本轮补充要求：检查时间状态“1条 · 每候选 3s（总计 0.1s）”与“总计”的计算/显示是否错误；这项必须与日志卡顿调查一起覆盖，不得只回答数字问题而漏掉前述识别故障任务。

## 日志中已确认的起始证据

- `/workspace/测试log/9.24.log`约286 KB；日志包含识别、棋局确认、分析请求、自动走子、落子核对及看门狗事件。
- 反复出现的状态为“1条 · 每候选 3s（总计 0.1s）”。抽取到34条这类我方计算状态；其相邻分析请求均发出`go movetime 100`，候选数为1。对应搜索有34次完成，运行耗时中位数约102ms、范围约17–113ms。故日志反映的实际引擎预算是100ms；“每候选3s”与引擎请求不一致，不能把标签直接当成实际搜索预算。
- 当前源码中`ScreenAssistService.scheduleAnalysis()`在构造实际预算后，计算状态文案的非深度分支固定写成“每候选”，并从独立的`perCandidateTimeMs`读取文案值；总预算则可能取`thinkTimeMs`。这是该矛盾的直接候选根因。100ms格式为`0.1s`本身是正确的，需修的是模式/预算/文案来源的一致性，并以实际提交的不可变预算生成标签。
- 日志另有落子后的识别重置与核对超时恢复：多次出现`MOVE_DISPATCH→VERIFYING→PIPELINE_WATCHDOG_ACTION RESET_VISION→REBASE_LANDING`，例如约第511–528行，派发后约1.3秒就进入重新建基线，随后才接受新画面；计划执行时需与用户指出的多次长卡顿逐一按时间戳、阶段和事件链关联，区分“识别错误/非法棋面”“采帧或推理未完成”“落子执行/核对超时”和“状态显示错配”。
- 9月24日日志中可见多轮“红帅数量异常:0”：至少有一轮持续约22.5秒、另一轮约24.4秒，期间每个稳定关键帧通常只有27或28子；另有约18.6秒、14.8秒、13.6秒和11.4秒的连续拒绝段。恢复时重新出现30/32子并继续分析，说明确实存在重复的识别拒绝/重试风暴；但日志没有原始截图、模型框、类别候选和置信度，暂不能仅凭日志证明具体是某个皮肤造成。
- 上述内容是第一轮读取所得，不能替代完整时间线分析；本计划不预设“皮肤”已被证明为根因。

本轮新增需求：允许用户在辅助页选择 YOLO 识别模型档位，并把模型能力、兼容性回退、功耗/速度提示和识别帮助纳入同一交付闭环；完整原话见 `request-addendum-01.md`。当前已取得并接入 Large 双模型集成工件（约54.26 MiB，输入 `[1,640,640,3]`、输出 `[1,25200,20]`），其来源与生成方式记录在 `docs/YOLO模型档位评估.md` 和 `tools/build_large_ensemble.py`；Medium 与 Lite 资产继续保留。

本轮新增严重识别错误证据：`/workspace/测试log/识别出错.log`（9378 bytes，当前已可读取）、`/workspace/测试log/识别出错.png`（3069399 bytes）、`/workspace/测试log/错误的识别结果。.png`（263612 bytes），并有 `/chat/attachments/375062.jpg`、`/chat/attachments/375063.png` 两份用户附件。当前日志确认：重置后先出现一次 `VISION_REJECT`，随后在 `stable=8` 时提交 `pieces=10` 的棋面 FEN；之后两次自动落子 `a1d1` 均进入 `VERIFYING`，约束看门狗先执行 `RESET_VISION`，随后执行 `REBASE_LANDING`，重新识别结果仍为同一10子FEN，且 `expectedMatch=false`；日志只提供聚合后的棋子数/FEN、锚点和方向，没有原始检测框、label、类别分数或第二候选，因此能确认识别/落子核对链路异常，但不能仅凭日志确定具体错类根因。图像作为复现样本纳入P8，用于对照真实棋盘与悬浮面板显示的棋子类别/位置；不能仅凭缩略图猜定具体错类。

完整检查9月24日日志中的多次长时间无进展及开局后自愈事件，定位证据可支持的根因并修复需要修复的软件路径；同时使每候选时间、总时、候选数、发给引擎的`go`预算与界面状态文案来源一致。新增P8还必须覆盖图像中这类严重棋子错类，不得只修漏检和卡顿。保留原识别安全门、连续8样本稳定要求、非法棋面拒绝、当前有效画面证据、搜索预算边界、落子校验和落子后回执。不能为消除卡顿或错类而无条件接受低置信/非法局面、缩短用户设定预算、沿用旧类别或让状态文案伪称实际预算。

## 原子任务、动作与验收

| 原子 | 行动 | 证据与验收 | 失败处置 |
| --- | --- | --- | --- |
| P1：保全并还原事件时间线 | 保留日志原件；按时间戳整理每个卡顿窗口的样本到达、稳定帧、推理开始/结果、棋盘接受/拒绝、搜索请求/完成、落子手势、阶段与看门狗事件；单独定位开局卡顿及自愈边界。 | 每个用户提到的长卡顿都有起止时间、持续时长、当时阶段、最后进展事件、恢复事件与相关日志行；分析文件/报告保留日志原行号。 | 若缺少可关联事件，标UNKNOWN并说明证据缺口；不把“皮肤识别”当作既定事实。 |
| P2：确认引擎预算与标签分歧 | 对照日志中全部`ANALYSIS_REQUEST`、候选数、请求时间及`STATUS`，检查实际写给引擎的预算与状态文案；审阅当前未经验证的`AnalysisSettingsSnapshot`变更（如仍存在），验证其是否进入真实调用链，否则不得留下无用代码。 | 用纯策略测试覆盖深度、总时、每候选时间及1/多候选：每类状态文字必须由同一份不可变预算生成，且与实际`go`命令一致；测试明确复现日志的100ms实际预算与旧文案冲突。 | 修复时保留各模式原语义；不通过降低预算来让数值看起来一致。 |
| P3：定位识别长卡顿根因 | 从日志定位每次阻塞在采帧队列、8样本稳定窗、ROI/整屏定位、TFLite推理、棋面合法性/身份约束、重置重试，还是落子核对；审阅检测器输入、框/类别/置信度和失败分支，判断是否存在皮肤导致的系统性漏检或错类。 | 每项根因对应日志证据、调用路径和可重复的最小测试输入；对可取得的日志/样本建立回归测试，覆盖低置信或缺子/缺将等错误结果仍被拒绝的安全负例。 | 若日志只有棋子数/FEN而没有原图、置信度或模型原始框，不能声称已证明特定皮肤根因；先修已证软件缺陷，并把该皮肤留作真机补充验证。 |
| P4：调查开局自愈事件 | 核对首次稳定棋面、引擎冷启动/NNUE预热、首个分析请求、首个PV、开局库命中/未命中、首个落子及识别重置的相对时间；确定属于冷启动正常延迟、看门狗误触发、合法性拒绝还是其他状态机卡点。 | 说明该事件的时间跨度与实际恢复触发；若能归因到代码错误，增加复现测试；若只属正常预热，明确标出而不做无依据改动。 | 根因不明时保留行为并列出缺少的设备数据，不能把“后来恢复”当成根因已经解决。 |
| P5：最小完整修复与文档 | 按P2/P3/P4的证据选择最小必要完整改动；状态文案使用与任务预算相同的不可变快照；识别修复不得绕过八样本稳定和非法棋面校验；同步必要的帮助/开发记录。 | 定向测试、源码审查、`git diff --check`通过；文案数值与日志/引擎命令同源；帮助没有与实际行为冲突的旧说明。 | 保留干净基线，按根因定点修复；失败不回滚整批此前用户改动，不扩展到无关功能。 |
| P6：回归、构建与交付 | 运行耗时策略/预算测试、相关识别管线与看门狗测试、全量JVM测试；执行ARMv8与ARMv8 dotprod clean Release及`lintVital`；检查APK版本、签名、库与帮助资源。生成独立本地测试候选及校验记录，不覆盖既有r23或正式Release。 | 逐项记录实际命令与结果；任何Linux/aarch64图形断言失败单列且不计通过；APK元数据、哈希、签名和内置帮助经实际复读核验。 | 构建/测试失败按根因最小修复并重验；没有真机不得宣称皮肤、真实录屏、引擎冷启动或自动落子体验已通过。 |
| P7：YOLO 模型档位、兼容性回退与说明 | Large/Medium/Lite 三档模型注册表、持久化选择、辅助页选择和兼容性探测已接入；默认新用户为 Large，已有用户保留记忆选择。Large→Medium→Lite 只在模型加载、张量分配、输入输出形状/类型或必要算子不兼容时回退；负载、温度、功耗和速度只作为提示。Large 已取得真实双模型集成工件，输入输出契约与后处理一致；来源、生成脚本和哈希已记录。 | 定向模型档位测试通过；主机 TFLite 已验证 Large 输入输出契约、FLOAT32 类型、540算子和零输入推理；帮助与评估文档已同步。真机复杂皮肤识别、设备兼容性、内存、速度、功耗和温升仍待设备验收。 | 模型切换失败按错误阶段记录并恢复旧档位或进入明确不可用状态；不得把 Large 集成档描述成独立训练的 YOLOv5-L，也不得把主机结果冒充真机准确率/功耗实测。 |
| P8：严重棋子类别错识别复现与修复 | 保全新增 `识别出错.log`、原始棋盘图和错误识别结果图；按时间戳关联 `VISION_RESULT`、聚合棋面、棋盘锚点、方向、映射结果、落子核对、`BoardSanitizer` 修正和最终 FEN；若后续取得原始检测框，再关联 label、置信度/类别边际和第二候选；用图像建立可重放样本，定位是模型类别混淆、标签映射、NMS/类别边际、裁剪/缩放、棋盘方向变换还是皮肤域差异。 | 新增样本在严格8帧稳定链路中不再把该棋子映射为错误类别；颜色、位置、朝向和FEN一致；自动落子前的己方棋子校验仍通过；同类负例、翻转棋盘、Large/Medium/Lite和低置信错类均有回归覆盖。当前日志只有聚合/FEN证据时，只能把模型原始类别根因标为UNKNOWN，不能宣称已修复真实错类。 | 原始日志已可读但没有模型框/类别分数时保留证据缺口，不猜测内容；禁止通过全局降低阈值、关闭结构校验、沿用旧类别或强行接受不确定类别来掩盖错误。 |
| P9：单候选变招临时预算通道 | 当用户配置候选数为1、变招处于预存状态且实际轮到我方时，运行时临时将候选数扩为2，不修改持久化设置；两条候选都必须得到与原单候选相同的预算：总时模式把原设定总时乘以2，每条等效原设定时间；每候选时间模式保持原单候选时间，实际总预算按2条计算；深度模式保持原深度，不人为附加时间。候选数大于1时不扩展、不增加预算。变招执行或取消后恢复用户原设置。 | 策略测试验证1→2、变招开/关、我方/对方回合、候选数>1；预算对象、`go`命令、状态文案与实际请求一致；例如原设定总时3s、候选1，临时请求为2条、总时6s，并能证明每条等效3s；取消或执行后恢复1条/3s。 | 不得修改用户持久化候选数、缩短原预算、用2条共享原3s冒充等价预算，或在候选数>1时额外翻倍；如触及引擎硬上限，必须保留实际限制证据并阻止伪称等价完成。 |
| P10：自动执子方的最小稳健判定 | 只用屏幕棋盘下半区（第5–9行）实际识别到的帅/将颜色作为己方依据：下半区唯一的帅/将为红则己方红，为黑则己方黑；不附加士/仕、相/象、兵/卒、布局姿态或历史棋面等并列确认条件。合法棋面的整体完整性由既有异常棋面校验负责；红帅或黑将缺失/重复、下半区没有或同时出现两方帅/将时，不猜测或写入自动颜色，立即走现有异常棋面重识别分支。 | 纯策略测试覆盖标准/翻转屏幕、普通棋子漏检时结果不变、单帅/将缺失或重复、下半区锚点缺失/歧义；服务测试/代码链确认无效棋面不会覆盖自动颜色或触发分析，稳定合法棋面后自动记忆同步更新，手动模式完全屏蔽该逻辑。 | 不把多个棋子类别、阵营线索或前一盘面状态叠成“必须同时通过”的识别条件；不可唯一确认时保留两套设置并走现有异常恢复，不回退到手动记忆猜阵营。 |

## P10自动执子方最小稳健判定任务定义

- 直接依据识别结果中的**屏幕坐标棋面**，取下半区第5至第9行作为我方半场（河界位于第4/5行之间）。该区域中红帅唯一存在时返回红方，黑将唯一存在时返回黑方；不根据canonical朝向标记、手动设置、士/仕/相/象/兵/卒、历史帧或多数票额外推断。
- 判定只使用帅/将这一类稳定阵营锚点；正常棋盘完整性/唯一性仍由已有`AssistBoard.validate`、宫位检查和结构门负责，不在执子方规则里重复构造多项同时满足的模型条件。其他棋子数量短暂少认但帅/将完整且位置有效时，己方颜色仍应唯一、稳定地判定。
- 若帅/将缺失、重复、屏幕下半区两方都出现、或均未出现，自动判定必须返回“不可判定”，不覆盖`autoSideRed`，不偷用`mySideRed`；在正常采纳链上由既有结构异常门拒绝该棋面、重置候选并立即请求新的稳定识别。若防御性检查在确认回调中发现异常，也复用相同异常重识别动作。
- 模式切换到自动时，如当前已有已确认且有效的棋面，立即以同一规则计算并同步自动记忆；如无有效棋面则保留先前自动记忆，显示等待有效棋面，不根据上一手动设置猜值。稳定的新棋面每次都以本帧屏幕下半区帅/将锚点更新自动记忆。
- 手动/自动共享已有模式选择，但两份颜色记忆独立：自动模式的分析、轮次解释、落子归属和状态展示只用自动检测值；手动模式只用手动记忆，完全不运行或写入自动判定路径。

### P10验收与回归

- 标准棋盘屏幕下半区的帅判为红；翻转棋盘屏幕下半区的将判为黑；对两种朝向删掉若干非帅/将棋子仍得到同一结果。
- 红帅/黑将任一缺失、任一重复、两者误落在屏幕下半区或两者都不在下半区时，策略不返回颜色；服务不得写入自动记忆、不得启动/继续分析或自动落子，并按已有异常棋面逻辑 rearm 稳定窗口、记录原因、强制重新识别。
- 自动模式的有效己方颜色只能来自这项单一屏幕半场锚点；手动红/黑记忆不得影响自动模式；自动检测记忆不得覆盖手动模式；模式切换与服务重建后分别恢复各自记忆。
- 检查最小条件没有扩展成多个棋子类别、前一帧类别、当前轮次、方向标签和置信度投票的并联确认要求；已有8稳定样本、全局结构合法性和异常棋面门原样保留。

## P7模型档位任务定义

### 模型档位与默认记忆

- `LARGE`：新增的高容量象棋 YOLO 模型，默认新用户首选；必须与当前 `YoloPostprocessor` 的输入、输出类别和棋盘映射契约逐项核对，若模型输出布局不同则单独适配，不能仅改文件名。
- `MEDIUM`：当前 `yolov5m_xq_fp32.tflite`，作为 Large 兼容失败后的第二档。
- `LITE`：当前 `yolov5n_xq_fp16.tflite`，作为最后回退档。
- 选择值写入 `AssistConfig`；没有历史值的新用户默认 `LARGE`，已有用户原来的中型/轻量选择不得被静默改写。当前服务初始化、重建服务、重新准备和模型切换都必须从同一持久化档位读取。

### 兼容性探测与回退边界

1. 手动在辅助页选择档位后，后台执行不阻塞界面的兼容性探测；探测至少包含资产存在、TFLite Interpreter 创建、`allocateTensors()`、输入形状 `[1,640,640,3]`、输入/输出 `FLOAT32` 类型、输出形状及类别维度、必要算子/运行时初始化。失败必须保留原始异常类别和可读原因。
2. 手动选择失败时按用户指定顺序从当前档位向下尝试：`LARGE → MEDIUM → LITE`；最终实际档位写回 `AssistConfig`，选择控件、状态提示和下一次准备读取的值同步更新。若 Lite 也失败，弹出明确的“设备无法加载任何识别模型”提示并阻止进入可识别运行态，不伪称已准备成功。
3. “一键准备”和悬浮窗“开始/继续”都必须在进入识别前检查当前记忆档位。默认 Large 即使用户没有打开设置也走同一检查链；失败时自动按顺序降级，并同步辅助页显示和悬浮窗/通知中的实际档位。
4. 兼容性只指当前设备/运行时能否加载并执行该模型契约。不得读取瞬时 CPU 占用、温度、电量或当前负载来强制降级；大型模型的功耗、发热和速度只在选择界面做温馨提示。只要兼容性探测通过，即使可能更慢或更耗电，也保留用户选择。
5. 模型切换必须先关闭旧 Detector 的生命周期闸门、等待在途推理退出、释放 Interpreter 和位图，再创建新档位；切换失败不得留下半关闭的识别服务或旧 UI 假称新模型。

### Large 与 Medium 的评估证据

- 文档至少包含：模型文件大小、输入/输出形状、参数量或可取得的模型元数据、推理线程配置、单帧冷启动/热身/稳定运行耗时、峰值内存、连续识别吞吐、功耗/温升观测方法和识别质量指标。
- 同一设备、同一 Android 版本、同一输入帧集、同一裁剪策略、同一 TFLite 线程数下比较 Large 与 Medium；质量至少统计棋盘框召回、棋子检测召回/精度、双王识别、完整合法棋面通过率和错误重试率。
- 结果分为“当前工具/模型实际测得”“由模型元数据推导”“必须连接真机才能测得”三类。当前Large工件已取得并完成主机契约/推理验证；缺少真机皮肤样本时，不能把主机结果写成Android准确率、功耗或温升实测。
- 用户帮助只写能直接指导选择的结论：Large 更重、通常更慢更耗电但预期识别能力更强；Medium 是平衡档；Lite 是兼容性和资源优先档；最终以设备兼容探测结果为准，负载提示不等于强制回退。

## P8严重棋子类别错识别任务定义

### 已登记的证据与当前限制

- 新增文件：`/workspace/测试log/识别出错.log`、`/workspace/测试log/识别出错.png`、`/workspace/测试log/错误的识别结果。.png`；新增附件：`/chat/attachments/375062.jpg`、`/chat/attachments/375063.png`。
- 复现图入口：![用户提供的棋盘样本](kelivo://workspace/%E6%B5%8B%E8%AF%95log/%E8%AF%86%E5%88%AB%E5%87%BA%E9%94%99.png) ![用户提供的识别结果](kelivo://workspace/%E6%B5%8B%E8%AF%95log/%E9%94%99%E8%AF%AF%E7%9A%84%E8%AF%86%E5%88%AB%E7%BB%93%E6%9E%9C%E3%80%82.png)。附件副本：![棋盘截图附件](kelivo://chat/attachments/375062.jpg) ![识别结果附件](kelivo://chat/attachments/375063.png)。
- 图像作为同一复现样本的“真实棋盘画面”和“悬浮面板识别结果”对照组保存；计划不把用户所说的错类直接写成具体棋子名称，必须以可读日志、原始检测输出或逐格图像标注确认。
- 当前 `识别出错.log` 已可读取；日志提供稳定帧、聚合棋面、锚点/方向、落子核对超时和重建基线证据，但不含原始检测框、label、类别分数或第二候选。解除模型层证据缺口的条件是取得原始检测输出或补充可重放的模型调试日志；原文件和图片不得覆盖、删除或改写。

### 复现与定位顺序

1. 读取日志原文并建立事件链：关联稳定帧序号、模型档位、`VISION_RESULT`、原始检测框数量、类别ID、置信度、类别第二候选、棋盘框/网格锚点、朝向、映射后的棋子、`BoardSanitizer` 修正说明、结构拒绝原因和最终FEN。
2. 对两张图做同坐标系标注：确认真实棋盘每个可见棋子的类别、颜色、格点和朝向，再与悬浮面板绘制的canonical棋盘逐格比较；区分“模型类别错”“格点映射错”“朝向翻转错”“颜色/红黑映射错”和“面板绘制错”，不能把显示错误误修成模型错误。
3. 对照代码链逐层复核：`YoloPostprocessor.decode` 的类别分数/边际与NMS、`YoloDetection.PIECE_CODES` 标签表、`DetectionBoardMapper` 的格点映射和方向转换、`BoardSanitizer` 的多认修正，以及 `ScreenAssistService` 对结构异常/缺子/非法局面的处理。
4. 如果日志带有原始类别候选，优先用同一输出重放；如果只有最终FEN和图像，则建立图像样本回归并把模型原始类别判定标为UNKNOWN。不得凭上一帧类别补齐或把用户指定执子方当作棋子类别判定依据。
5. 只有当根因被证据区分后才选择修复：标签表错误修标签表；坐标/朝向错误修映射；类别边际/NMS误删修后处理并保留安全门；皮肤域导致的真实低置信错类则评估模型/样本/专用候选策略，不能全局放宽阈值。

### P8验收与安全边界

- 新图像样本在连续8帧稳定后，逐格类别、颜色、位置、朝向和FEN与人工标注一致；悬浮面板与识别提交使用同一canonical结果。
- 错类样本不得触发错误的己方棋子判断、建议着法或自动落子；自动落子前的 `AssistBoard.isLegalMoveInModel`、起点归属检查和结构安全门继续有效。
- 增加至少一个该样本的正例回归、一个相邻类别易混淆负例、一个翻转朝向负例、一个低置信不确定类别负例；验证Large、Medium、Lite的标签契约一致，不能只修当前档位。
- 修复后再次对照日志：错误类别不再写入提交FEN；若仍不能从当前证据证明修复，交付状态标为“图像/日志证据已登记，根因或真机效果待验证”，不宣称完全解决。

## P9单候选变招临时预算任务定义

### 触发、预算与生命周期

- 触发条件必须同时满足：用户持久化候选数为1、`nextVariationArmed` 已预存、当前实际轮到我方；对方回合、尚无棋面、候选尚未返回时只保留预存意图，不提前消耗或伪造第二候选。
- 临时通道只改变当前分析请求的有效候选数，不写回 `AssistConfig.candidateCount`，不改变用户下次设置；候选数为2至6时完全沿用用户设置和预算，不进入该特殊分支。
- 总时模式：设用户原设定总时为 `T`，临时请求使用2条候选和 `2T` 的实际引擎总时；这样两条候选各自获得与原来唯一候选相同的 `T` 预算，实际 `go movetime` 必须反映 `2T`。
- 每候选时间模式：设用户原设定每候选时间为 `P`，临时请求仍使用每候选 `P`，候选数为2，实际总时为 `2P`；不能把临时通道再额外乘一次。
- 深度模式：临时请求候选数为2但保持用户原深度，不凭空添加时间上限；变招预算的“相同时间”要求仅适用于存在时间预算的模式。
- 变招实际非默认落子完成核对后，或用户再次点击取消后，下一次分析恢复用户候选数和原预算；清除请求前不能把临时结果当作永久设置。
- 当前源码初查显示 `effectiveCandidateCount` 已能把候选1临时扩为2，但 `AnalysisBudget.forTotalTime` 仍按原 `totalMs` 生成命令；P9必须补齐总时翻倍和状态显示，不能只扩候选数量。

### P9验收与回归

- `configured=1, armed=true, myTurn=true`：有效候选数为2；总时3s时提交 `go movetime 6000`，状态文案显示实际2条/总计6s及等效每条3s；每候选3s模式也提交总时6s；深度模式只提交原深度。
- `configured=1, armed=false`、`myTurn=false`、候选数大于1、变招取消、变招执行后：均不得临时翻倍；命令和状态恢复到用户原配置。
- 对候选1、候选2、总时模式、每候选模式、深度模式分别覆盖纯策略测试；测试临时请求在候选暂缺时保持等待，候选出现后才进入可执行状态。
- 检查状态文案必须来自实际不可变 `AnalysisBudget`，日志中的 `ANALYSIS_REQUEST`、`goCommand()` 和悬浮窗显示不能互相矛盾；不得只改显示文字而保留旧预算。
- 检查预算上限：若现有引擎/API上限会截断 `2T`，必须先取得硬上限证据，再选择提高该特殊路径的可支持上限或明确阻止该次特殊请求；不得静默截断后宣称两条候选均获得原预算。

## 依赖与顺序

1. 本计划与本轮原话已先落盘；执行前复核 `request.md`、`request-addendum-01.md`、`request-addendum-02.md`、`request-addendum-03.md`、`request-addendum-04.md`、`request-addendum-05.md` 和计划状态；本轮用户已明确取消计划审阅等待，复读通过后立即执行。
2. P1建立按时间排序的证据表；P2已完成源码审计并进入预算/显示定向测试；P3可继续按日志证据审计识别链路。
3. P3与P8共享识别证据，但分开验收：P3处理漏检、非法棋面和卡顿链路；P8处理具体棋子类别/身份错识别。新增日志已可读取，但原始检测框和类别分数仍缺失，若执行中无法取得则把模型层根因标为UNKNOWN，不阻塞已可验证的代码修复。
4. P3/P8按原始模型框、类别分数、图像和FEN选择最早失效环节；仅在证据足以区分标签映射、几何映射、后处理或皮肤域差异时修改对应路径，不能先改全局阈值。
5. P4独立调查启动与自愈；其结论不得与P3/P8混并，除非共享同一已证根因。
6. P7已取得并核验 Large 集成工件、来源/许可证、输入输出契约和哈希；不再把Large资产缺失作为阻塞。
7. P9先完成预算策略、状态文案和变招生命周期的定向实现与测试；P10修正自动执子方判定并验证异常分支；再与P5整合，P7、P8、P9、P10的代码/文档通过定向验证后，P6执行最终全量测试、双变体构建、提交、推送和Release验收。
8. 最终检查改动范围、未完成项、设备侧依赖、版本/签名/工件边界，再交付。

## 风险、回滚与执行边界

- 主要风险是把识别漏检或错类错误归因于特定皮肤，在证据不足时放宽合法性、类别边际或稳定性门；任何识别调整必须同时有正例与安全负例回归。
- 新增 `识别出错.log` 已可读取；它提供稳定帧、聚合棋面、落子核对超时和重建基线证据，但没有原始检测框/label/置信度。若执行中仍无法取得模型层证据，P8只能把具体类别根因和真机效果标为UNKNOWN，不能声称已完成具体错类修复；原始图片可作为回归样本。
- 严重错类可能直接改变己方棋子归属、候选着法和自动落子目标；任何修复必须先证明类别/映射根因，并保留8帧稳定、结构合法、低置信拒绝和落子前归属校验。
- `100ms`实际预算与`3s`状态文案是独立可观察的矛盾；确认配置为总时模式且预算确为100ms后，预期修复是状态显示“1条共享总时0.1s”，而不是把引擎预算暗改成3s。若配置模式实际是每候选模式，则应按同一请求快照显示其真实单候选和总预算，不能猜模式。
- 先保存原始日志与当前源码基线；每个代码改动保持可逆。普通测试失败不得全量回滚；本轮用户已明确授权提交、推送和创建`v1.3`正式Release。
- 工作区当前有此前尝试加入的`AnalysisSettingsSnapshot`代码（来源于上一轮未验证编辑）；执行前需先审阅工作区状态与差异，确认其与已发布基线的关系，将其视为候选未验证改动而非已完成修复。
- 真正皮肤样本、录屏权限、设备型号/刷新率、真实手势和长时间运行复测需目标Android设备；缺样本时只能交付代码与日志证据层面的结论并列出待设备验证项。
- Large 工件已取得并接入为真实双模型集成档：`app/src/main/assets/yolov5l_xq_fp32.tflite`，输入输出契约与当前后处理一致；不得把它描述成独立训练的YOLOv5-L权重，必须保留Medium+universal逐锚点择优的来源说明。
- 设备负载、温度和功耗属于用户提示与评估数据，不是兼容性失败条件；只有 Interpreter/张量/必要算子/受控探测执行失败才触发档位回退。回退原因必须可追溯到具体阶段和异常。
- 模型切换会影响 native 内存和在途推理；必须先关闭旧 Detector 并通过生命周期门验证释放，再加载新模型，失败时保留可运行的旧档位或进入明确不可用状态，禁止半切换。

## 完成标准

P1已完成日志时间线初查，P2已完成预算显示定点修复并待当前测试结果闭合；P3–P6仍需继续执行。P7已完成模型档位注册表、持久化选择、辅助页模型控件、真实Large集成工件、兼容性探测、Large→Medium→Lite回退接口、失败提示、评估文档和三处帮助内容的源码接入；Large为Medium+universal逐锚点择优集成，不冒充独立训练YOLOv5-L。P8已取得新增识别日志和图像样本，已确认稳定识别后落子核对超时与重建基线链路，但原始模型类别证据不足，具体错类根因需代码/样本/真机证据闭合或明确UNKNOWN。P9已加入计划并开始实现，要求候选1变招临时扩为2并在时间模式下为新增候选提供等价预算。P10已纳入计划：用屏幕下半区唯一帅/将作自动执子方的单一阵营锚点；缺帅/将或异常下半区按现有异常棋面通道重识别，不猜测、不污染自动记忆。当前最终测试、P8证据闭合、P9预算回归、P10自动执子判定回归、双变体构建、远程提交和Release发布需继续闭合；真机兼容性、模型切换、复杂皮肤识别、严重错类复现、前台切换恢复、双执子方策略、内存、速度、功耗和温升仍需设备验收。所有可定位卡顿窗口及开局自愈事件逐项得到结论或明确UNKNOWN；计时状态文字与不可变分析预算一致且回归测试覆盖总时/每候选模式；P9的临时`go`命令和状态文案与实际预算一致；P10在小量普通棋子漏检时仍能稳定判色、帅/将基础异常时拒绝并重识别；识别修复不放松稳定帧及非法棋面防线；最终候选文件、元数据、签名、哈希与源码一致；真实 Release 中正式 APK 和校验文件已公开上传；真机未验证项清楚列出。
