plan 然后加一条任务进去：
这个yolo检测模型的选择放到ui界面可以用户自选默认为中型，选中后探测设备是否支持，当用户设备不支持就弹出弹窗提示并且告知原因然后自动回设为lite的，还有种就是用户没看设置直接是默认的（那这个时候我们一键准备和开启后都是一样检测是否支持不支持就自动降级，对应ui界面要同步更新设置的为lite，

然后还加一个大型的yolo吧就是比当前中型大一档的用于提升识别能力，默认也改成选择大型（这个选项是记忆的）

大型不行就中型，中不行就lite（这里是检测设备是否兼容不是检测设备负载这个只作为用户选择时的温馨提示例如选择大型会加剧功耗和温度上升但不禁止，只要设备可以跑那就不自动回退，对应ui界面要同步更新设置的为lite，

然后你评估一下这个大型对比中型模型能力区别，功耗，速度区别给我出成文档文件，然后也要写进我们软件的相关帮助页面里面当选项的解释。
