plan mode 

还有我这边不是说过，记录下点击开始这个按钮所在的应用，前台应用 APP，前台全屏展示的应用 APP。然后当应用发生切换，也就是同样的 8 个稳定帧过去，确认切换了应用，那此时就自动进行一个暂停。然后回到原来的页面的时候，就自动恢复我们切应用前的一个状态。比如说我切应用出去，这个时候我设置的是开始的，也就是一个正在运行中的状态，那么我切回来也要恢复正在运行中的状态。然后如果说我切出去的时候，它是本来就是一个暂停的状态，那那么我切回来它就也是恢复暂停的状态，或者说可以叫做不恢复成运行态。因为我们切出去是暂停态，那那么那么我们切回来，我们其实只需要再次设定为暂停态，就是二次去保障它确实处于我们切出去的状态就行。

还有我之前所提到的，当我这个悬浮球已经展开成悬浮面板的时候，我们的通知我到现在也没看到它同步更新的状态，就是展开为面板的时候，你的通知也不能再写成当前还是悬浮球的状态，你得同步呀

还有目前有个很大的问题，就是我们这里面，我说了要遵循我们这个悬浮窗，悬浮面板里面那一个己方的直子颜色为最高标准，但是我刚刚实测就是假设我这边就是我面向的第一个是红紫，那么红紫是我模拟在之子，然后对面是我一个朋友，他黑子，然后现在我希望嗯，他用也就是说我设置的面板是黑色，就是我们是黑色，这就是这个这个辅助它用黑，结果就是因为我这边是正向的，一个一个棋盘，所以它默认它可能内部还有一个检测机制，它认为它目前用的是一个红子，这与面板产生冲突，然后有时候他就会拿着我的棋子去下，这是有问题的，因为我们说了他，我们给他面板上设置的是什么样的执子方他就要严格遵循，不会自己去加以判定，要把原有的那个原身判定理原生判定给关掉或者全部屏蔽掉，要以我们的为准 plan加到原有，或者说现在正在执行的计划中的，后面追加进去执行

然后然后那对于上面的那我们就不进行完全的裁切吧，就是我们分两套执行逻辑。我们在 UI 界面，就是我们应用内部的那个调整界面，悬浮窗辅助里面的界面，可以去选择我们的启动方式。一种启动方式是手动选择执棋方，还有一种就是自动检测执棋方。就是，这是两个互斥选项，你可以做成滑动开关，滑到哪边就是哪边，这会影响我们悬浮窗出来的布局。

如果选择的是自动检测，那这个手动选择执棋方就不能够进行一个，就是它并不是一个按钮，它是实时去显示我们当前所检测到的执棋颜色。就是它不能点了，它被变得一个禁用状态，然后可以明显看出这个是不能点击的按钮，就是它是一个半透明的感觉，定在那里，它用来去动态的显示我们的执棋方。如果是手动，那么我们这个悬浮窗面板，就是我们这一次生成的，就是一个可以去切换的。然后这个是可以实时生效的，就是我们在 UI 界面改了悬浮面板，立马就会刷新出对应的一个布局状态。比如说我切换成了自动检测，那这边悬浮面板自动的实时给这个按钮禁用，然后切换另一套逻辑。然后两个逻辑都不能够有交叉干扰，就是我假设说我用手动，那么自动检测的逻辑要被完全屏蔽掉，就是落实到整个周期的每一处都是屏蔽掉自动检测的一个逻辑，全部走我们手动检测逻辑，严格按照我们手动指定的去思考、去落子、去一个计算。然后如果是自动检测逻辑，那么我们手动检测的状态和记忆都不影响我们自动检测的这个逻辑，全部走自动检测。也就是说，自动检测逻辑这里的执子方和我们手动检测逻辑的执子方，不是手动设置逻辑的手动设置执子方的存储是两套不同的存储。就是我们切换逻辑，我们都要恢复到对应的设置的那套逻辑的那个记忆值。你能懂我意思吗？就是这是两套不同的策略下的不同的存储记忆值。这是一项大工程，所以我需要你理清楚列计划，然后才开工。